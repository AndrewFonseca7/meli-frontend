import React from "react";

/**
 * Componente que renderiza la página de rutas no encontradas.
 */
const NotFound = () => (
	<div className='not-found'>
		<h3 className='mx-auto col-10'>
			No hemos podido encontrar lo que buscabas.
		</h3>
	</div>
);

export default NotFound;
